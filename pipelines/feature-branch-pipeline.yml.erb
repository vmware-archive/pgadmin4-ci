jobs:
- name: <%= render_branch_name %>-javascript-tests
  plan:
  - task: say-hello
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: fotomut/pgadmin4, tag: 'python-3.6'}
      run:
        path: echo
        args: ["from dynamic pipeline"]
